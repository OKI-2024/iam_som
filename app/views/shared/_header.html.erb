<header>
      <div class="navbar">
        <div class="nav-left">
          <ul class="nav"><a href="#">アイムソムとは？</a></ul>
          <ul class="nav"><a href="#">ワ活</a></ul>
          <ul class="nav"><a href="#">ワインニュース</a></ul>
        </div>
        <div class="nav-right">
          <% if user_signed_in? %>
          <ul><%= link_to 'ログアウト', destroy_user_session_path, data: {turbo_method: :delete}, class: "log-out" %></ul>
          <ul class="notification"><a href="#">通知</a></ul>
          <% else %>
          <ul><%= link_to '新規登録', new_user_registration_path, class: "sign-in" %></ul>
          <ul><%= link_to 'ログイン', new_user_session_path, class: "log-in" %></ul>
          <%  end %>
          
          
          <% if current_user %>
          <ul class="icon"><%= link_to image_tag(current_user.avatar, class:"icon"), edit_user_path(current_user) %></ul>
          <% end %>
        </div>
      </div>
    </header>
